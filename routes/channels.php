<?php

/*
|--------------------------------------------------------------------------
| Broadcast Channels
|--------------------------------------------------------------------------
|
| Here you may register all of the event broadcasting channels that your
| application supports. The given channel authorization callbacks are
| used to check if an authenticated user can listen to the channel.
|
*/

// Broadcast::channel('App.User.{id}', function ($user, $id) {
// //    return (int) $user->id === (int) $id;
//    return true;
// });


// Broadcast::channel('tickets.{id}', function ($user, $id) {
//     return true; // only auth user will subscribe to this channel
// //    return $user->id == \App\Models\Site\Ticket::findOrFail($id)->user_id || $user->guard('agent'); // only the owner of the ticket or the admins
// });
